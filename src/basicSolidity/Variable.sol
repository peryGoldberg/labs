// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract Variables{
    uint256 public i;

    function doing() public{
        uint256 num=90;
        address sender=msg.sender;
        uint256 stap=block.timestamp;
    }
}